{
  "name": "ben-anderson-portfolio",
  "version": "1.0.0",
  "private": true,
  "description": "Ben Anderson's portfolio, built with Eleventy (11ty).",
  "scripts": {
    "dev": "eleventy --serve",
    "start": "eleventy --serve",
    "build": "eleventy && pagefind --site _site"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "pagefind": "^1.1.0"
  }
}
